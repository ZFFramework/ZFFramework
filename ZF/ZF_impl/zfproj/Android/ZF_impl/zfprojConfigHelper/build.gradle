plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace 'com.ZFFramework.zfprojConfig.ZF_impl'
    compileSdk 35
    sourceSets {
        main {
            if (file(this.projectDir.getAbsolutePath() + '/../zflib/zfprojConfig/zfprojConfig.xml').exists()) {
                manifest.srcFile this.projectDir.getAbsolutePath() + '/../zflib/zfprojConfig/zfprojConfig.xml'
            }
        }
    }
}

